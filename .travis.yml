language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t ashafir/docker-react -f Dockerfile.dev .

script:
  - docker run ashafir/docker-react npm run test -- --coverage
